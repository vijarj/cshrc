Download VNC server and run it:
sudo dpkg -i VNC-Server-6.9.1-Linux-x64.deb
Open GUI VNC server and set it up

To install GNOME desktop:
sudo apt-get update
sudo apt-get install kali-desktop-gnome

To set shortcuts in gnome:
Menu-settings-keyboard-view and Customizable shortcuts
